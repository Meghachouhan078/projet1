/*package arraysprogram;

public class SortAscendingDescending {
    public static void main(String[] args)
    {
        int[] arr = {6,8,4,2,9,2,7};
        int n = arr.length;
        printOrder(arr, n);
    }
    static void printOrder(int[] a, int n)
    {
        int temp;
        for(int i=0;i < n-1;i++) {
            for (int j = 0; j < n / 2; j++) {
                if (a[j] > a[j + 1]) {
                    temp = a[j];
                    a[j] = a[j + 1];
                    a[j + 1] = temp;
                }
            }
            System.out.print(a[i] + " ");
        }}}
          //  for(int j = n/2;j < n-1; j++)
           // {
             //   if(a[j] < a[j+1])
              //  {
                //    temp=a[j];
                //    a[j]=a[j+1];
                  //  a[j+1]=temp;
                //}
           // }
       // }

       // for(int i = 0; i < n; i++)
           // System.out.print(a[i] + " ");

    //}
   // }*/


